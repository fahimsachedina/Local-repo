ANTHROPIC_API_KEY=your_api_key_here

# Comma-separated list of allowed frontend origins (set in production).
# Use * to allow all origins (fine for local dev, not recommended in prod).
ALLOWED_ORIGINS=*
