// Set this to your deployed backend URL before deploying the frontend.
// Example: window.API_URL = 'https://my-salary-api.railway.app';
// Leave as empty string only if the frontend and backend share the same domain.
window.API_URL = 'http://localhost:8000';
